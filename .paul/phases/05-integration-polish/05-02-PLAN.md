---
phase: 05-integration-polish
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [lobe/inbox/status-polish-pass.md]
autonomous: false
---

<objective>
## Goal
Run the full quality pipeline on the status ecosystem -- fix all build errors and lint warnings, task Lobe with a visual polish pass across every new status page and component, then checkpoint for Anthony's visual review before shipping.

## Purpose
Every prior phase added features. This plan adds finish. Build errors that slipped through incremental development get caught. Lint warnings that accumulated across 14 plans get cleaned. Visual inconsistencies between components built in different sessions get harmonized. This is the difference between "it works" and "it ships" -- the final gate before the status ecosystem is production-ready.

## Output
- Clean `npm run build` (zero errors)
- Clean `npm run lint` (zero warnings in status-related files)
- Lobe task brief at `lobe/inbox/status-polish-pass.md` for visual consistency, spacing, and mobile responsiveness review
- Checkpoint note for Anthony's final visual review
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
- Phase 1: Schema, mutations, queries, expiration cron -- all in `app/convex/`
- Phase 2: User path system -- `userPaths` table, path selection UI, multi-path support
- Phase 3: Toggle UI -- `StatusToggleCard`, `StatusPanel`, `ActiveStatusBadges`, `StatusHistory`, `/status` page
- Phase 4: Discovery -- `discoverStatuses`, `getDiscoveryPaths`, `getComplementaryMatches`, connection flow, `/discover` page, `DiscoveryCard`, `MatchCard`
- Phase 5 (05-01): Public integration -- `getPublicStatusSummary`, `PublicStatusBadges`, `PostStatusIndicator`, badges on barber profile, shop website, and feed

## Files to Audit
All status ecosystem files created across phases 1-5:
- Backend: `app/convex/statuses.ts`, `app/convex/statusConfig.ts`, `app/convex/schema.ts` (status tables)
- UI components (Lobe-created): `StatusToggleCard`, `StatusPanel`, `ActiveStatusBadges`, `StatusHistory`, `PathSelector`, `DiscoveryFeed`, `DiscoveryCard`, `MatchCard`, `PublicStatusBadges`, `PostStatusIndicator`
- Pages: `app/(auth)/status/page.tsx`, `app/(auth)/discover/page.tsx`
- Integration points: `app/barber/[slug]/page.tsx`, `app/shop/[userId]/page.tsx`, feed post cards

## Source Files
@app/convex/statuses.ts
@app/convex/statusConfig.ts
@app/convex/schema.ts
</context>

<acceptance_criteria>

## AC-1: Build passes with zero errors
```gherkin
Given the full codebase with all status ecosystem changes
When `npm run build` is run from app/
Then it completes with zero TypeScript errors
And zero build-time errors
And the output confirms successful compilation
```

## AC-2: Lint passes with zero warnings in status-related files
```gherkin
Given the full codebase with all status ecosystem changes
When `npm run lint` is run from app/
Then there are zero warnings in any status-related files
And any pre-existing warnings in non-status files are noted but not required to fix
```

## AC-3: Lobe polish task brief is comprehensive
```gherkin
Given the plan is complete
When lobe/inbox/status-polish-pass.md is read
Then it contains a complete Lobe task brief with:
  - execute mode header
  - Explicit list of every status-related page and component to review
  - Checklist for design consistency (fonts, colors, spacing, border radius)
  - Checklist for mobile responsiveness (320px, 375px, 768px breakpoints)
  - Checklist for empty states and loading states
  - Checklist for dark mode compatibility (if applicable)
  - Instructions to screenshot each page after polish
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Run build and lint, fix all errors</name>
  <files>any files with errors</files>
  <action>
    Run the full build and lint pipeline from `app/` and fix every error and warning.

    **Step 1: Build**
    ```bash
    cd app && npm run build
    ```
    If the build fails:
    - Read each error carefully. Most will be TypeScript type errors or missing imports.
    - Fix each error in the source file. Do NOT suppress errors with `// @ts-ignore` or `any` types unless absolutely unavoidable.
    - Re-run `npm run build` after each round of fixes until it passes cleanly.

    **Step 2: Lint**
    ```bash
    cd app && npm run lint
    ```
    If lint reports warnings or errors:
    - Fix all warnings in status-related files (any file touching the status ecosystem: statuses.ts, statusConfig.ts, status pages, status components, discovery pages, integration points).
    - Pre-existing warnings in unrelated files should be noted in the summary but do NOT need to be fixed in this plan.
    - Re-run `npm run lint` after fixes until status-related files are clean.

    **Step 3: Type check**
    ```bash
    cd app && npx tsc --noEmit
    ```
    This catches errors that the build might not surface (unused variables, type mismatches in non-rendered code paths). Fix any errors found.

    Document every error found and how it was fixed in the task summary.
  </action>
  <verify>Run `npm run build` from app/ -- must pass with zero errors. Run `npm run lint` from app/ -- must pass with zero warnings in status-related files. Run `npx tsc --noEmit` from app/ -- must pass.</verify>
  <done>AC-1 and AC-2 satisfied: build, lint, and type check all pass cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Write Lobe task brief for visual polish pass</name>
  <files>lobe/inbox/status-polish-pass.md</files>
  <action>
    Create a Lobe task brief at `lobe/inbox/status-polish-pass.md` with the following spec:

    Header:
    ```
    <!-- execute -->
    <!-- max-turns: 60 -->
    ```

    Brief content:

    **What:** Final visual polish pass across every page and component in the status ecosystem. Review for design consistency, spacing rhythm, mobile responsiveness, and overall cohesion. This is the last touch before shipping.

    **Pages to review (visit each, inspect, fix):**
    1. `/status` -- the status management page (PathSelector, StatusPanel, StatusToggleCards, StatusHistory)
    2. `/discover` -- the discovery feed (path filter tabs, DiscoveryCard grid, MatchCard section)
    3. `/barber/{any-slug}` -- public barber profile with status badges (PublicStatusBadges integration)
    4. `/shop/{any-userId}` -- public shop website with status badges (ShopTemplate integration)
    5. `/home` -- community feed with PostStatusIndicator on post cards

    **Components to review:**
    - `StatusToggleCard` -- toggle switch, expiration countdown, refresh button
    - `StatusPanel` -- path group headers, toggle grid, empty states
    - `ActiveStatusBadges` -- inline badge rendering
    - `StatusHistory` -- archived status rows, timestamps, duration badges
    - `PathSelector` -- path selection cards, multi-path support
    - `DiscoveryFeed` / `DiscoveryCard` -- path filter tabs, status cards with user info
    - `MatchCard` -- complementary match display
    - `PublicStatusBadges` -- shared badge component (default and compact modes)
    - `PostStatusIndicator` -- feed post status indicator

    **Design consistency checklist:**
    - [ ] All heading styles use the same font family and weight hierarchy (font-heading for section headers, font-mono for labels/metadata)
    - [ ] All status labels use `font-mono`, `text-[10px]` or `text-[11px]`, lowercase
    - [ ] All timestamps use `font-mono`, `text-[10px]`, `text-muted-foreground/60`
    - [ ] All badges use consistent border-radius (`rounded-full` for status pills)
    - [ ] All Card components use the same border, shadow, and padding patterns
    - [ ] Color tokens are consistent -- `text-foreground`, `text-muted-foreground`, `bg-foreground/5` for badge backgrounds
    - [ ] No hardcoded colors (hex/rgb) -- all colors use CSS variables or Tailwind tokens
    - [ ] Empty states follow the same pattern: centered, muted text, lowercase, single line

    **Spacing & rhythm checklist:**
    - [ ] Consistent vertical spacing between sections (`mt-8` or `mt-12` between major sections)
    - [ ] Consistent gap in grids and flex layouts (`gap-3` or `gap-4`)
    - [ ] Page padding is consistent with other (auth) pages (`px-6 py-8` or the established pattern)
    - [ ] No cramped areas -- everything breathes (design principle: "Space is a design element")

    **Mobile responsiveness checklist (test at 320px, 375px, 768px):**
    - [ ] All pages render correctly at 320px width (smallest mobile)
    - [ ] Toggle cards stack vertically on mobile, grid on desktop
    - [ ] Discovery cards: single column on mobile, 2-col on md+
    - [ ] Path filter tabs scroll horizontally on mobile without overflow issues
    - [ ] Status badges wrap naturally on narrow screens
    - [ ] No horizontal overflow on any page at any breakpoint
    - [ ] Touch targets are at least 44px for interactive elements

    **Loading & empty states:**
    - [ ] Every query-driven component handles the loading state (shows skeleton or nothing, not a flash of "no data")
    - [ ] Every list/grid has an empty state message
    - [ ] Empty states are helpful, not just blank space

    **After polish:**
    - Take a browser screenshot of each page listed above (desktop and mobile widths)
    - If any page has visual issues that cannot be fixed within the turn limit, document them in `lobe/outbox/status-polish-report.md`

    **Design constraints:**
    - Do NOT introduce new design patterns -- harmonize with what exists
    - Do NOT add animations or transitions that do not already exist on the page
    - Do NOT change the information architecture or layout structure -- only polish spacing, fonts, colors, and responsive behavior
    - Fix issues, do not redesign
  </action>
  <verify>Confirm the file exists at lobe/inbox/status-polish-pass.md and contains execute header, max-turns header, list of all pages and components, design consistency checklist, spacing checklist, mobile responsiveness checklist, loading/empty state checklist, and screenshot instructions</verify>
  <done>AC-3 satisfied: Lobe task brief contains comprehensive visual polish spec covering every status page, every component, with explicit checklists for design, spacing, mobile, and states</done>
</task>

<task type="human">
  <name>Task 3: Anthony's visual review checkpoint</name>
  <files>none</files>
  <action>
    This is a manual checkpoint. After Tasks 1 and 2 are complete and Lobe has executed the polish brief:

    1. Run `npm run dev` from `app/` to start the dev server
    2. Visit each page in the browser:
       - http://localhost:3100/status (status management)
       - http://localhost:3100/discover (discovery feed)
       - http://localhost:3100/home (community feed with status indicators)
       - http://localhost:3100/barber/{your-slug} (public profile with badges)
       - http://localhost:3100/shop/{your-userId} (shop website with badges)
    3. Check mobile rendering (browser dev tools, 375px width)
    4. Confirm the status ecosystem feels cohesive across all surfaces
    5. If approved: mark Phase 5 complete in ROADMAP.md and STATE.md
    6. If issues found: write a targeted fix task for Lobe

    This task blocks the milestone from being marked complete. Anthony's eyes on the render is the final gate.
  </action>
  <verify>Anthony confirms visual approval or files follow-up tasks</verify>
  <done>Anthony has visually reviewed all status ecosystem pages and confirmed they are ship-ready</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- app/convex/schema.ts (unless fixing a genuine type error -- no structural changes)
- app/convex/statusConfig.ts (locked since Phase 1)
- app/convex/crons.ts
- app/convex/auth.ts
- Any file outside the status ecosystem (unless it has a build error directly caused by status changes)

## SCOPE LIMITS
- Task 1 fixes build/lint/type errors ONLY -- no feature additions, no refactoring
- Task 2 is a Lobe task brief ONLY -- do not build or edit UI directly
- Task 3 is a human checkpoint -- no automated action
- Polish means consistency and correctness, NOT redesign
- If build errors exist in files unrelated to the status ecosystem, note them but do NOT fix them in this plan (out of scope)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes from app/ with zero errors
- [ ] `npm run lint` passes from app/ with zero warnings in status-related files
- [ ] `npx tsc --noEmit` passes from app/
- [ ] `lobe/inbox/status-polish-pass.md` exists with execute header and comprehensive spec
- [ ] All build/lint fixes are documented
- [ ] Task 3 (human checkpoint) is clearly defined with specific URLs and review criteria
- [ ] No feature additions -- only error fixes and polish brief
</verification>

<success_criteria>
- Tasks 1 and 2 completed (Task 3 is human-gated)
- All verification checks pass
- No errors introduced
- The codebase compiles cleanly with zero errors and zero warnings in status-related files
- Lobe's polish brief is thorough enough to catch visual inconsistencies across all 14 plans' worth of UI work
- The human checkpoint provides a clear, actionable review workflow
- After this plan completes (including Task 3), the v0.1 Status Ecosystem milestone is shippable
</success_criteria>

<output>
After completion, create `.paul/phases/05-integration-polish/05-02-SUMMARY.md`
</output>
